<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Detalhes da Imagem</title>
    <link rel="stylesheet" href="/styles/image-details.css" />
  </head>
  <body>
    <div class="content">
      <div class="details">
        <h1>Detalhes da Imagem</h1>
        <p>ID: <%= photo.id %></p>
        <p>Título: <%= photo.title %></p>
        <p>Descrição: <%= photo.description %></p>
        <p>Data de Criação: <%= photo.createdAt %></p>
        <p>Tags: <%= photo.tags %></p>
        <p>Número de likes: <%= likes.length %></p>
        <p>Likes:</p>
        <ul>
          <% if (likes.length === 0) { %>
          <li>A imagem não possui nenhum like </li>
          <% } else { %> <% likes.forEach(like => { %> <% users.forEach(user =>
          { %> <% if (like.userId === user.id) { %>
          <li>Nome: <%= user.name %> | Username: <%= user.username %></li>
          <% } %> <% }) %> <% }) %> <% } %>
        </ul>
      </div>
      <div class="image">
        <img
          src="/uploads/<%= photo.imageUrl %>"
          width="500px"
          height="500px"
        />
      </div>
    </div>
  </body>
</html>
